/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "images.unsplash.com",
      "i.pravatar.cc",
      "source.unsplash.com",
      "randomuser.me",
    ],
  },
};

export default nextConfig;
